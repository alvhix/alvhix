---
import { formatDate } from '@/utils/formatDate';

interface Props {
  date: Date;
}

const { date } = Astro.props;
const formattedDate = formatDate(date);
---

<time datetime={date?.toISOString() ?? undefined}>{formattedDate}</time>
